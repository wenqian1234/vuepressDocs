{
  "name": "docs",
  "version": "1.0.0",
  "description": "'# Hello VuePress'",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "docs:dev": "vuepress dev docs",
    "docs:build": "vuepress build docs",
    "deploy-gh": "GH=1 npm docs:build && bash scripts/deploy.sh",
  "deploy": "bash deploy.sh"
    "deploy:build":"npm run docs:build &&master -d .vuepress/dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "homepage": "https://wenqian1234.github.io/docs/",
  "devDependencies": {
    "gh-pages": "^3.1.0"
  }
}
